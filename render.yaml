services:
  - type: web
    name: medkb-ai
    env: python
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: cd src && uvicorn web_server:app --host 0.0.0.0 --port $PORT
    plan: free
